CREATE DATABASE raw_accidents_db;
\c raw_accidents_db;
CREATE TABLE caracteristics (
    id SERIAL PRIMARY KEY,
    -- TODO: Add additional columns as from the csv files
);
CREATE TABLE holidays (
    id SERIAL PRIMARY KEY,
    -- TODO: Add additional columns as from the csv files
);
CREATE TABLE places (
    id SERIAL PRIMARY KEY,
    -- TODO: Add additional columns as from the csv files
);
CREATE TABLE users (
    id SERIAL PRIMARY KEY,
    -- TODO: Add additional columns as from the csv files
);
CREATE TABLE vehicles (
    id SERIAL PRIMARY KEY,
    -- TODO: Add additional columns as from the csv files
);